extends layouts/main.pug

block content
    .user-profile
        .user-profile_header
            img.avatar(src=loggedUser.avatarUrl)
            h4.profile_username=loggedUser.name
            h3=loggedUser.email
            